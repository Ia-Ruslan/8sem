# - Config file for the MYCSR package
# It defines the following variables
#  MYCSR_INCLUDE_DIRS  - include directories for MYCSR and dependencies
#  MYCSR_LIBRARIES     - libraries to link MYCSR
#  MYCSR_VERSION       - version of MYCSR

# @<название переменной>@ означает, что это место при 
# генерации конфиг-файла надо поставить значение 
# переменной <название переменной> из CMakeLists.txt
set(MYCSR_VERSION @MYCSR_VERSION@)

# следующая строка нужная для генерации нескольких функций, 
# например, функции check_required_components
@PACKAGE_INIT@

# подключаем файл с экспортируемыми целями
# мы сгенерируем его в CMakeLists.txt
# CMAKE_CURRENT_LIST_DIR - путь к директории,
# где лежит обрабатываемый CMake в данный момент файл
include ( "${CMAKE_CURRENT_LIST_DIR}/MyCSR-targets.cmake" )

# эти переменные, как и MYCSR_VERSION можно было и не определять
# это сделано скорее для примера
#set(MYCSR_INCLUDE_DIR "@PACKAGE_MYCSR_INCLUDE_DIR@")
#set(MYCSR_LIBRARIES "@MYCSR_LIBRARIES@")

check_required_components(MyCSR)